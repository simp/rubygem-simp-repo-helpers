RSpec.describe Simp::Repo::Helpers do
  it 'has a version number' do
    expect(Simp::Repo::Helpers::VERSION).not_to be nil
  end

  it 'does something useful', skip: "it's too early to tell"  do
    expect(false).to eq(true)
  end
end
